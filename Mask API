pip install covidcast
from datetime import date
import covidcast

data = covidcast.signal("fb-survey", "smoothed_wearing_mask_7d",
                        date(2021, 2, 9), date(2023, 1, 8),
                        "state")
